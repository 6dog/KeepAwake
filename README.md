# Keep Awake

A small macOS menu bar app that prevents the Mac from sleeping while it is enabled.

## Build

```sh
./scripts/build_app.sh
```

The script builds the Swift executable and stages `Keep Awake.app` in this directory.
